/** Shared UI constants (design-language consistency across views). */

/** How many recent items (projects / PDFs) a sidebar list shows before the
 *  "全部 (N)" fold — shared by ProjectTree and the PDF library so both stay
 *  visually consistent. */
export const RECENT_TOTAL = 10
